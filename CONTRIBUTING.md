# 参与贡献

感谢你帮助改进腕上导航。请先阅读 [README](README.md) 和 [开发环境](docs/development.md)，了解手机 0.2.1、手环 0.2.4 的适用范围与验证状态。

## 开始开发

需要 Git 和 Node.js 22+（推荐 24）。根目录没有第三方 npm 依赖，无需 `npm install`：

```sh
npm test
npm run check:public
npm run dev
```

打开 <http://127.0.0.1:4173/> 操作手环页面。贡献界面、协议和测试不需要手机、签名或小米 SDK。Android 改动按开发文档补齐 JDK/SDK 后执行无签名测试、Lint 与编译；维护者负责使用私有原签名打包。不要索取或提交签名私钥。

## 报告问题

使用 Issue 模板，填写双端版本、手机系统、手环型号/固件、运动健康与高德版本、最短复现步骤，以及期望和实际结果。把“SDK 发送成功”“收到 ACK”“实际看到手环画面”分别说明。浏览器预览结果请标注为模拟验证。

导航文字统一替换为“示例道路”“直行200米”等虚构内容。截图应裁去通知栏、账号和路线；不要上传原始通知、完整系统日志、设备序列号、AuthKey 或签名文件。安全问题请阅读 [SECURITY.md](SECURITY.md)。

## 提交改动

1. Fork 后创建自己的功能分支，一次 PR 聚焦一个问题，说明触发条件和改动后的行为。
2. 保留 `com.autonavi.minimap` 过滤与原文兜底；不要猜测转向、距离、道路或路线。
3. 分别处理同步启停、导航结束、断连、过期、隐藏和销毁；自动打开按一次导航去重。
4. 对行为变化增加有意义的测试，运行相关检查；纯文档变动无需重新构建安装包。
5. 提交前运行 `npm run check:public` 和 `git diff --check`，核对文件列表；不要使用 `git add -f` 绕过私有文件忽略规则。
6. PR 中列出已验证的内容与待真机验证的边界。涉及第三方代码或资源时同时提供来源及许可。

本地代理协作还应遵循 [AGENTS.md](AGENTS.md)。主仓库维护工作使用 `main`；贡献者可在自己的 Fork 分支提交 PR。

提交原创贡献表示你有权提供这些内容，并同意按项目 [MIT 许可证](LICENSE) 分发；第三方部分保留原有许可证。
