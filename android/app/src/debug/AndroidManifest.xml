<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- ADB-only development controls. Ordinary apps do not hold DUMP permission. -->
        <receiver android:name=".DiagnosticReceiver" android:exported="true" android:permission="android.permission.DUMP" />
    </application>
</manifest>
